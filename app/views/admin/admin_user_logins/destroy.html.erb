<div class="block">
  <h2>Delete User Login</h2>
  <% form_tag(admin_user_login_url(@user_login), :method => :delete) do %>
    <div class="form-row">
      <span class="data">Are you sure you want to delete this user login?</span>
    </div>
    <div class="form-row">
      <span class="data"><%= submit_tag('Delete') %></span>
      <span class="data"><%= cancel_button %></span>
    </div>
  <% end %>
</div>
