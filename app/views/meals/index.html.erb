<div class="block">
  <h2>Today's Meals</h2>
  <%= print_flash(flash[:error], 'error', 'Error') -%>
  <% if @meals.size > 0 -%>
  <%= render(:partial => @meals) -%>
  <% else -%>
  <p>You have not entered any meals for today.</p>
  <% end -%>
</div>
